# Architecture

## Project Structure and Build System

The library is organized into multiple backends, each in its own source
directory and its own Cabal sublibrary:

| Sublibrary | Source directory | Contents |
| --- | --- | --- |
| `simdy:common` | `src-common/` | Backend-independent code (type classes, shuffle, fusible) |
| `simdy` | `src/` | Public API (`Data.Simdy`, `Pack`, `Mask`, `Shuffle`, ...) and the `Data.Simdy.Vector.*` stream-fusion layer |
| `simdy:no-simd` | `src-no-simd/` | Scalar fallback (no SIMD instructions) |
| `simdy:vl128` | `src-vl128/` | 128-bit vectors (x86 SSE, Arm NEON) |
| `simdy:vl256` | `src-vl256/` | 256-bit vectors (x86 AVX2) |
| `simdy:vl512` | `src-vl512/` | 512-bit vectors (x86 AVX-512) |
| `simdy:vl128-c`, `simdy:vl256-c`, `simdy:vl512-c` | `cbits/` | C implementations, one sublibrary per vector length (see [C FFI](#c-ffi-cbits)) |
| `simdy:plugin` | `src-plugin/` | The shuffle type-checker plugin |

Cabal flags (`no-simd`, `haswell`, `avx512`, `llvm`) decide which backends are
buildable on the target platform. The *default* vector length is then selected
with `mixins:`, which renames the chosen backend module to
`Data.Simdy.Internal.Default`:

```cabal
mixins: simdy:vl256 (Data.Simdy.Internal.VL256, Data.Simdy.Internal.VL256 as Data.Simdy.Internal.Default)
```

Every module in `src/` imports `Data.Simdy.Internal.Default`, so the public API
follows the selected backend without any CPP.

## Key GHC Extensions

### TypeFamilies

- **Data families** define vector types with backend-specific representations:
  `data instance X4 Float = MkFloatX4 FloatX4#`
- **Type families** associate related types:
  `type family Mask a` maps element types to their mask representation,
  and `type family HalfVector x` maps vector types to their half-width counterparts.

### QuantifiedConstraints

The `SIMD` class uses quantified constraints to express that properties of
element types lift to vector types:

```haskell
class ( ...
      , forall a. SIMDNum a => Num (x a)
      , forall a. SIMDEq a => Equatable (x a)
      , ...
      ) => SIMD x
```

This means a single `SIMD x` constraint is sufficient to use all standard
type class operations on `x a`, as long as the element type `a` has the
appropriate `SIMD*` constraint.

## Hardware Vector Length vs Element-Count-Based API

GHC's SIMD primops are named after hardware vector lengths (e.g. `FloatX4#`
for 128-bit, `FloatX8#` for 256-bit). The library instead exposes an
**element-count-based** API (`X4`, `X8`, `X16`, `X32`, `X64`) that is
independent of the hardware vector width.

The same type `X8 Float` has different internal representations depending
on the backend:

- **vl128**: two `FloatX4#` fields (composed from 128-bit vectors)
- **vl256**: a single native `FloatX8#`

This decoupling allows user code to be written in terms of element counts
and ported across backends by simply switching the import.

## Supplementing Primitives Not Provided by GHC

As of GHC 9.14, GHC's SIMD primops cover basic arithmetic (`+`, `-`, `*`, `/`), FMA, and min/max,
but do not provide abs, sqrt, bitwise operations, comparisons, or select
(`abs`, `sqrt`, and certain bitops are added in GHC 10.0).
The library fills these gaps using two strategies:

### Relying on LLVM Optimization

Operations like `abs` and `sqrt` are written as scalar-per-element expansions:

```haskell
absFloatX4# u = case unpackFloatX4# u of
  (# u0, u1, u2, u3 #) -> packFloatX4# (# fabsFloat# u0, fabsFloat# u1, fabsFloat# u2, fabsFloat# u3 #)
{-# INLINE [0] absFloatX4# #-}
```

With `INLINE [0]`, these are inlined at the final simplifier phase and passed
to the LLVM backend, whose SLP vectorizer reassembles them into single SIMD
instructions.

### C FFI with Platform Intrinsics

Bitwise operations (complement, and, or, xor), shifts, select, and
comparisons are implemented as C functions using SIMD intrinsics directly
(`_mm_and_si128` / `vandq_s8` etc.) and called via `foreign import ccall`.
Variants are provided for each ISA level (baseline / Haswell / AVX-512);
see [C FFI (cbits/)](#c-ffi-cbits) for how those variants are kept ABI-safe.

### Floating-Point min/max

Although GHC 9.12+ provides min/max primops for vectors, the library implements its
own floating-point min/max in C to ensure IEEE 754-2019 compliant NaN handling.

## Code Generation Scripts

Many source files are generated by scripts (indicated by a first-line comment
`-- This file was created by script/<name>.hs. Do not edit by hand!`):

- `script/Gen.hs`: Generates vector type modules (X2--X64) and their
  instances for all supported element types, across all backends.
- `script/GenVL.hs`: Generates public API modules (e.g.
  `Data.Simdy.Internal.VL128`) with summary classes (`SIMDElement`,
  `SIMDNum`, etc.), the `SIMD` class, shuffle wrappers, and rewrite rules.
- `script/GenPrim.hs`: Generates primop wrappers (`Prim.hs`) that provide
  operations not available as GHC primops.

## Type Class Hierarchy and DerivingVia

### F-Suffixed Classes and WrappedMulti

Core operations are defined in "F-suffixed" classes parameterized by the
vector type constructor:

- `NumF x a`, `FractionalF x a`, `FloatingF x a` (arithmetic)
- `EquatableF x a`, `OrderedF x a` (comparison)
- `BooleanF x a`, `BitShiftF x a` (bitwise)
- `MinMaxF x a`, `FusedMultiplyAddF x a`

The `WrappedMulti x a` newtype bridges them to the standard classes:

```haskell
deriving via WrappedMulti X4 a instance NumF X4 a => Num (X4 a)
```

### Summary Classes

Backend-specific modules define summary classes (e.g. `SIMDNum`) that bundle
the F-suffixed constraints for all vector widths, simplifying user-facing
type signatures.

## HalfVector and Horizontal Fold

The `HalfVector` type family and `SplitShortVector` class enable recursive
decomposition of vectors:

```haskell
type family HalfVector x    -- e.g. HalfVector X8 = X4
class SplitShortVector x a where
  splitShortVector :: x a -> (HalfVector x a, HalfVector x a)
```

`horizontalFold` uses this to reduce a vector to a scalar by recursively
splitting and combining halves, bottoming out at `Identity`:

```haskell
-- Identity
horizontalFold _ = runIdentity
-- X2 (halves are Identity)
horizontalFold op v = case splitShortVector v of
  (low, high) -> runIdentity (op low high)
-- X4 and wider
horizontalFold op v = case splitShortVector v of
  (low, high) -> horizontalFold op (op low high)
```

## Stream Fusion

The library uses stream fusion (similar to the `vector` library) for bulk
vector operations.

### Bundle

```haskell
data Bundle m x a = forall s. MkBundle
  { sMultiStep     :: s -> m (s, x a)   -- SIMD-width step
  , sStep          :: s -> m (s, a)     -- scalar step (remainder)
  , sInitialState  :: s
  , sSize          :: !Int
  }
```

The existential state `s` enables stateful iteration without exposing the
state type.

### Rewrite Rules and Phase Control

- `stream`/`unstream` rewrite rules eliminate intermediate allocations:
  `stream (new (unstreamAsNew s)) = s`
- `INLINE [1]` on public functions allows rules to fire before inlining.
- `INLINE [0]` on inner step functions ensures they are inlined at the
  final phase.

## Rewrite Rules

Beyond stream/unstream fusion, the library uses rewrite rules for:

- **`liftSIMD` specialization**: Common `liftSIMD coerce` patterns are
  rewritten to direct constructor calls (e.g. `liftSIMD coerce = mkSum @X4`).
- **FMA fusion** (via `Data.Simdy.Fusible`): The module defines
  its own `(+)`, `(-)`, and `(*)` operators (to be qualified-imported) with
  `INLINE [0]`, so that rewrite rules can match on them before they are
  inlined to the Prelude definitions. Patterns like `a F.* b F.+ c` and
  `a F.- b F.* c` are rewritten to `fusedMultiplyAdd` when FMA is
  enabled.

## Shuffle System

GHC's shuffle primops (e.g. `shuffleFloatX4#`) require their index arguments
to be **compile-time constants**. The library wraps this constraint using
a type class and a compiler plugin:

- **`ShuffleMany`**: A "magic" type class with no hand-written instances.
  Its method `shuffleMany# :: (Natural -> v) -> v` takes a source-vector
  selector and produces the shuffled result.
- **Compiler plugin** (`Data.Simdy.Shuffle.Plugin`): A `TcPlugin` that
  solves `ShuffleMany` constraints by reading type-level `[Natural]` indices
  and generating Core expressions that call the appropriate shuffle primop
  (GHC 9.12+) or fall back to `unpack`/`pack`.
- **`RequiredTypeArguments`** (GHC 9.10+) allows shuffle indices to be
  passed as type-level tuples with clean syntax.

## C FFI (cbits/)

The `cbits/` directory contains C implementations for operations that benefit
from direct use of SIMD intrinsics: bitwise logic, shifts, select, comparisons,
floating-point min/max (IEEE 754-2019), and mask pack/unpack.

### Why the C sources are split by vector length: an LLVM ABI problem

The obvious arrangement -- one file per operation holding all vector lengths,
with `__attribute__((target("avx2")))` on the wide functions -- **breaks under
LLVM**.

This is an LLVM-specific problem. In LLVM (Clang), the `target` attribute
changes instruction selection *inside* a function body, but the calling
convention used for the function's vector arguments and return values is derived
from the *translation unit's* target features, which the attribute does not
affect. So in a TU compiled without `-mavx2`, a `__m256i` argument or return
value is passed in memory even for a function marked
`target("avx2")`, while a caller compiled with `-mavx2` passes it in a `ymm`
register. Nothing diagnoses the mismatch; the code simply computes garbage or
crashes at run time.

The same mismatch can arise across the FFI boundary itself, because the Haskell
callers of these functions are code-generated by LLVM too: the 256- and 512-bit
backends require `-fllvm`, and the vector calling convention LLVM picks there
likewise follows the target features in effect for the module. Both sides must
agree, and a per-function attribute cannot make them agree. The split below is
therefore mandatory, not a stylistic choice.

The split happens at two levels:

- **One file per length**: `*_vl128.c`, `*_vl256.c`, `*_vl512.c`.
- **One Cabal sublibrary per length**, each with its own `cc-options`, so that
  every file is compiled in a TU whose baseline makes its vector length native:

  | Sublibrary | `cc-options` |
  | --- | --- |
  | `vl128-c` | SSE2 baseline; `-mavx2 -mfma -mf16c` and `-mavx512*` are added when the `haswell` / `avx512` flags are set |
  | `vl256-c` | `-mavx2 -mfma -mf16c` (always) |
  | `vl512-c` | the above plus `-mavx512f -mavx512vl -mavx512bw -mavx512dq` |

  `vl128`, `vl256` and `vl512` each depend on the matching `*-c` sublibrary.

### What the target attribute is still used for

Within a file, `target` opts individual functions into ISA extensions that do
*not* change that file's vector ABI. For example, `bitwise_vl128.c` defines

```c
__attribute__((target("avx512f,avx512vl")))
__m128i hs_simdy_shiftRInt64X2_avx512(__m128i x, HsInt y)
```

which is safe because the ABI of `__m128i` is already fixed by the SSE2
baseline. Choosing between the baseline and the extended variant happens on the
Haskell side, via CPP in `src-vl*/Data/Simdy/Internal/VL*/PrimExtra.hs`
(`USE_AVX512`, `__SSE4_1__`).

As a second line of defence, each file is wrapped in a feature guard
(`#if defined(__SSE2__)`, `#if defined(__AVX2__)`,
`#if defined(__AVX512F__) && defined(__AVX512BW__)`), so building it without the
required flags yields an empty translation unit instead of a compile error.

A side benefit of the split: the C test suites (`float-minmax-test`,
`double-minmax-test`) can link all three length sublibraries at once.

## Compiler and Platform Workarounds

Which primops exist, and which of them work, depends on the GHC version, the
code generator (LLVM or NCG) and the target architecture. Two places carry that
knowledge:

- `script/GenPrim.hs` emits `MIN_VERSION_GLASGOW_HASKELL` guards around the
  operations that only became primops in later GHCs (integer vector min/max,
  bitwise `and`/`or`/`xor`, `abs`, `sqrt`), falling back to scalar expansions or
  the C implementations otherwise.
- The header comment of `simdy.cabal` lists the GHC releases that are excluded
  and the bugs behind them (broken FMA, `__GLASGOW_HASKELL_LLVM__` detection,
  FFI on AArch64, `+evex512`, NCG spilling, x86 stack alignment), together with
  links to the GHC issues. Consult it before widening a version bound.
